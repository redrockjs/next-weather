export enum RoutesEnum {
  HOME = '/',
  FAVORITES = '/favorites',
  SETTINGS = '/settings',
  SIGNIN = '/signin',
  SIGNUP = '/signup',
  RESTORE = '/restore',
  CITY = '/city',
}

export enum BackendRoutesEnum {
  SIGNIN = '/account/sessions/email',
  GET_ACCOUNT = '/account',
}
